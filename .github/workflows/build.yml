name: Build and Test

on:
  push:
    branches: [ master, main ]
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'
  pull_request:
    branches: [ master, main ]
  workflow_dispatch:

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run unit tests
        run: ./gradlew test --no-daemon --stacktrace

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: app/build/reports/tests/

  build:
    name: Build APK
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build debug APK
        run: ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Build release APK
        run: ./gradlew assembleRelease --no-daemon --stacktrace

      - name: Sign release APK
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          # Decode the keystore from base64 secret
          echo "${{ secrets.SIGNING_KEYSTORE }}" | base64 -d > release.keystore

          # Sign the APK
          $ANDROID_HOME/build-tools/36.1.0/apksigner sign \
            --ks release.keystore \
            --ks-key-alias ${{ secrets.SIGNING_KEY_ALIAS }} \
            --ks-pass pass:${{ secrets.SIGNING_KEYSTORE_PASSWORD }} \
            --key-pass pass:${{ secrets.SIGNING_KEY_PASSWORD }} \
            --out app/build/outputs/apk/release/app-release-signed.apk \
            app/build/outputs/apk/release/app-release-unsigned.apk

          # Replace unsigned with signed
          mv app/build/outputs/apk/release/app-release-signed.apk app/build/outputs/apk/release/app-release.apk

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: calsynx-debug
          path: app/build/outputs/apk/debug/app-debug.apk

      - name: Upload release APK
        uses: actions/upload-artifact@v4
        with:
          name: calsynx-release
          path: app/build/outputs/apk/release/app-release*.apk

  release:
    name: Create Release and Update F-Droid Repo
    runs-on: ubuntu-latest
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Checkout F-Droid branch
        run: |
          git fetch origin fdroid:fdroid || git checkout --orphan fdroid
          git checkout master

      - name: Download release APK
        uses: actions/download-artifact@v4
        with:
          name: calsynx-release

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install fdroidserver
        run: |
          pip install fdroidserver

      - name: Setup F-Droid repository structure
        run: |
          # Create fdroid directory structure
          mkdir -p fdroid/repo

          # Extract version from gradle
          VERSION=$(grep 'versionName' app/build.gradle.kts | sed 's/.*versionName = "\(.*\)"/\1/')
          echo "VERSION=$VERSION" >> $GITHUB_ENV

          # Copy signed APK to repo (prefer signed over unsigned)
          if [ -f app-release.apk ]; then
            cp app-release.apk fdroid/repo/calsynx-${VERSION}.apk
          else
            cp app-release-unsigned.apk fdroid/repo/calsynx-${VERSION}.apk
          fi

      - name: Initialize or update F-Droid repository
        run: |
          cd fdroid

          # Create config
          cat > config.yml << 'EOF'
          repo_name: Calsynx F-Droid Repository
          repo_description: Personal F-Droid repository for Calsynx calendar sync app
          repo_url: https://raw.githubusercontent.com/${{ github.repository }}/fdroid/repo
          archive_name: Calsynx Archive
          archive_description: Archived versions of Calsynx
          EOF

          # Update repo (creates index without signing the repo itself)
          fdroid update --nosign --create-metadata

          cd ..

      - name: Deploy F-Droid repo to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./fdroid
          publish_branch: fdroid
          enable_jekyll: false

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            app-release.apk
            app-release-unsigned.apk
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
